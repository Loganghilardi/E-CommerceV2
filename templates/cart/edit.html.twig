{% extends 'base.html.twig' %}

{% block title %}{{'EditCart'|trans}}{% endblock %}

{% block body %}
    <h1>{{'EditCart'|trans}}</h1>

    {{ include('cart/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cart_index') }}">{{'BackToList'|trans}}</a>

    {{ include('cart/_delete_form.html.twig') }}
{% endblock %}
